spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: root
        platform: mysql
        url: jdbc:mysql://localhost:3307/iwi_db
        username: root
    jpa:
        generate-ddl: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true